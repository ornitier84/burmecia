---
settings:
  semaphores:
    debug: ".vagrant/tmp/.debug"
    verbose: ".vagrant/tmp/.verbose"
    environment:
      initialized: ".initialized"
    machine:
      keys_provisioned: ".keys_provisioned"